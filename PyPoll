import os, csv

input_file = path("python-challenge", "PyPoll")
Election Data = {'Voter ID': ['Total Months',],
        'County': ['Total Months',],
        'Candidate': ['Total Months',],
}
df = pd.DataFrame (data, columns = [])

print (df)
 

